<%= form_with(model: related_program, local: true) do |form| %>
  <% if related_program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(related_program.errors.count, "error") %> prohibited this related_program from being saved:</h2>

      <ul>
        <% related_program.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :parent %>
    <%= form.number_field :parent %>
  </div>

  <div class="field">
    <%= form.label :child %>
    <%= form.number_field :child %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
