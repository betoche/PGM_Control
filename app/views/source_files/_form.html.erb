<%= form_with(model: source_file, local: true) do |form| %>
  <% if source_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(source_file.errors.count, "error") %> prohibited this source_file from being saved:</h2>

      <ul>
        <% source_file.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :library_id %>
    <%= form.text_field :library_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
